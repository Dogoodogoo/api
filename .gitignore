# ==============================================================================
# 1. Java & Spring Boot (빌드 및 시스템 파일)
# ==============================================================================
*.class
*.log
/build/
/out/
/target/
.gradle/
build/

# ==============================================================================
# 2. 보안 및 시크릿 설정 (가장 중요)
# ==============================================================================
# 모든 설정 파일은 기본적으로 무시 (보안 우선 원칙)
**/*.yml
**/*.yaml
**/*.properties
.env
secret.py

# [예외] 오직 기본 설정 파일인 application.yml만 추적 허용
# (반드시 위 ignore 패턴들보다 아래에 위치해야 합니다)
!src/main/resources/application.yml

# IntelliJ 환경 설정 및 개인 테스트 파일
.idea/
*.iml
src/test/http/http-client.private.env.json

# ==============================================================================
# 3. Gradle Wrapper (빌드 필수 파일은 유지)
# ==============================================================================
!gradle/wrapper/gradle-wrapper.jar

# ==============================================================================
# 4. OS 및 기타
# ==============================================================================
.DS_Store
Thumbs.db